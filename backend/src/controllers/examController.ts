import { NextFunction, Request, Response } from 'express';

export const getExams = (req: Request, res: Response, next: NextFunction) => {};

export const createExam = (req: Request, res: Response, next: NextFunction) => {};

export const editExam = (req: Request, res: Response, next: NextFunction) => {};

export const deleteExam = (req: Request, res: Response, next: NextFunction) => {};
