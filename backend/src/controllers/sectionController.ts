import { NextFunction, Request, Response } from 'express';

export const getSections = (req: Request, res: Response, next: NextFunction) => {};

export const createSection = (req: Request, res: Response, next: NextFunction) => {};

export const editSection = (req: Request, res: Response, next: NextFunction) => {};

export const deleteSection = (req: Request, res: Response, next: NextFunction) => {};
