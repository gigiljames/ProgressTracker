import { NextFunction, Request, Response } from 'express';

export const getChapters = (req: Request, res: Response, next: NextFunction) => {};

export const createChapter = (req: Request, res: Response, next: NextFunction) => {};

export const editChapter = (req: Request, res: Response, next: NextFunction) => {};

export const deleteChapter = (req: Request, res: Response, next: NextFunction) => {};
